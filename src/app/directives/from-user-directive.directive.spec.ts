import { FromUserDirectiveDirective } from './from-user-directive.directive';

describe('FromUserDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FromUserDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
